package com.zos.auth.repository;

import com.zos.auth.model.AppToken;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

public interface AppTokenRepository extends JpaRepository<AppToken, String> {
    Optional<AppToken> findTopByUseridOrderByExpiresdateDesc(String userid);
}
